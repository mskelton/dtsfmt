== should format ==
//foo
  //  bar   

[expect]
// foo
// bar

== should re-align with code ==
// Layers

#define DEFAULT 0

[expect]
// Layers
#define DEFAULT 0

== comments inside nodes can be formatted ==
/ {
  device { // comment1
    compatible = "device";
         // comment2
    property;
  };
};

[expect]
/ {
  device {
    // comment1
    compatible = "device";

    // comment2
    property;
  };
};

== no empty lines between { and comments ==
/ {
  device {
    
    
        // comment
    compatible = "device";
  };
};

[expect]
/ {
  device {
    // comment
    compatible = "device";
  };
};
